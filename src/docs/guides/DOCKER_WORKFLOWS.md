# 🔄 Docker Workflows - Step-by-Step Processes

## Visual Workflows for Common Tasks

---

## 🌅 Daily Development Workflow

```
┌─────────────────────────────────────────────────────────┐
│                    START YOUR DAY                       │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Navigate to Project                             │
│   cd d:\buildsite-flow                                   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Start All Services                              │
│   docker compose -f docker-compose.dev.yml up -d        │
│                                                          │
│   What happens:                                          │
│   ✓ PostgreSQL starts (database)                        │
│   ✓ Redis starts (cache)                                │
│   ✓ Backend starts (API server)                         │
│   ✓ Frontend starts (React app)                          │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Verify Everything Started                       │
│   docker compose -f docker-compose.dev.yml ps           │
│                                                          │
│   Expected: All services show "Up" and "healthy"        │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Open Browser                                     │
│   http://localhost:5173                                 │
│                                                          │
│   ✓ Frontend loads                                      │
│   ✓ Can log in                                          │
│   ✓ Everything works                                    │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│                    START CODING! 🎉                     │
└─────────────────────────────────────────────────────────┘

Total Time: 2-3 minutes
```

---

## 💻 Making Code Changes Workflow

```
┌─────────────────────────────────────────────────────────┐
│              MAKING CODE CHANGES                         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Edit Your Code                                  │
│   - Edit files in src/ (frontend)                       │
│   - Edit files in server/ (backend)                     │
│   - Save files                                          │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Check if Hot Reload Works                       │
│   - Frontend: Changes appear in browser automatically   │
│   - Backend: Server restarts automatically              │
│                                                          │
│   ✓ If working: You're done!                            │
│   ✗ If not working: Continue to Step 3                │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Manual Restart (if needed)                     │
│   docker compose -f docker-compose.dev.yml restart backend│
│   docker compose -f docker-compose.dev.yml restart frontend│
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Verify Changes                                  │
│   - Refresh browser                                      │
│   - Test functionality                                   │
│   - Check logs if issues                                │
└─────────────────────────────────────────────────────────┘

Time: 2-10 seconds (if hot reload works)
Time: 30 seconds (if manual restart needed)
```

---

## 📦 Adding New Dependencies Workflow

```
┌─────────────────────────────────────────────────────────┐
│          ADDING NEW NPM PACKAGE                         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Add Package to package.json                     │
│   - Edit package.json                                   │
│   - Add: "new-package": "^1.0.0"                       │
│   - Save file                                           │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Rebuild Container                               │
│   docker compose -f docker-compose.dev.yml build backend │
│                                                          │
│   What happens:                                          │
│   ✓ Reads package.json                                   │
│   ✓ Runs npm install                                     │
│   ✓ Installs new package                                 │
│   ✓ Creates new image                                    │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Restart Service                                  │
│   docker compose -f docker-compose.dev.yml up -d backend │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Verify Package Works                            │
│   - Use package in code                                 │
│   - Check logs for errors                               │
└─────────────────────────────────────────────────────────┘

Time: 2-5 minutes
```

---

## 🗄️ Database Backup Workflow

```
┌─────────────────────────────────────────────────────────┐
│              BACKING UP DATABASE                        │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Check Database Size                            │
│   docker compose -f docker-compose.dev.yml exec postgres │
│     psql -U postgres -c "SELECT pg_size_pretty(          │
│       pg_database_size('buildflow_db'));"                │
│                                                          │
│   Purpose: Know how long backup will take               │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Create Backup                                   │
│   docker compose -f docker-compose.dev.yml exec postgres │
│     pg_dump -U postgres buildflow_db >                  │
│     backup_$(date +%Y%m%d_%H%M%S).sql                   │
│                                                          │
│   What happens:                                          │
│   ✓ Connects to database                                │
│   ✓ Exports all data to SQL file                        │
│   ✓ Saves to current directory                           │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Verify Backup File                              │
│   ls -lh backup_*.sql  (Linux/Mac)                      │
│   dir backup_*.sql      (Windows)                       │
│                                                          │
│   Check: File size should be > 0                        │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Store Backup Safely                             │
│   - Copy to external drive                              │
│   - Upload to cloud storage                             │
│   - Keep multiple versions                              │
└─────────────────────────────────────────────────────────┘

Time: 30 seconds - 5 minutes (depending on database size)
```

---

## 🔄 Database Restore Workflow

```
┌─────────────────────────────────────────────────────────┐
│            RESTORING FROM BACKUP                        │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Verify Backup File Exists                       │
│   ls backup_20250119.sql  (Linux/Mac)                   │
│   dir backup_20250119.sql  (Windows)                     │
│                                                          │
│   ⚠️ Make sure you have the right backup file!         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Backup Current State (Safety First!)           │
│   docker compose -f docker-compose.dev.yml exec postgres │
│     pg_dump -U postgres buildflow_db >                   │
│     backup_before_restore_$(date +%Y%m%d).sql            │
│                                                          │
│   Why: In case restore fails, you have current data      │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Stop Services (Optional - for safety)            │
│   docker compose -f docker-compose.dev.yml stop backend │
│                                                          │
│   Why: Prevents connections during restore              │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Restore Database                               │
│   docker compose -f docker-compose.dev.yml exec -T postgres │
│     psql -U postgres buildflow_db < backup_20250119.sql │
│                                                          │
│   What happens:                                          │
│   ✓ Reads SQL file                                       │
│   ✓ Drops existing tables (if any)                      │
│   ✓ Creates tables                                       │
│   ✓ Inserts all data                                     │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Verify Restore                                  │
│   docker compose -f docker-compose.dev.yml exec postgres │
│     psql -U postgres -d buildflow_db -c                  │
│     "SELECT COUNT(*) FROM agencies;"                    │
│                                                          │
│   Check: Should match expected data count                │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 6: Restart Services                                 │
│   docker compose -f docker-compose.dev.yml start backend │
│   docker compose -f docker-compose.dev.yml start frontend│
└─────────────────────────────────────────────────────────┘

Time: 1-10 minutes (depending on backup size)
```

---

## 🚀 Production Deployment Workflow

```
┌─────────────────────────────────────────────────────────┐
│         PRODUCTION DEPLOYMENT                            │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Prepare Environment File                        │
│   cp .env.production.example .env.production            │
│   nano .env.production  (edit with your values)          │
│                                                          │
│   Required changes:                                      │
│   ✓ POSTGRES_PASSWORD (strong password)                 │
│   ✓ VITE_JWT_SECRET (generate with openssl)              │
│   ✓ VITE_API_URL (your domain)                          │
│   ✓ CORS_ORIGINS (your domains)                         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Build Production Images                         │
│   docker compose -f docker-compose.prod.yml build        │
│                                                          │
│   What happens:                                          │
│   ✓ Builds optimized frontend (React → static files)    │
│   ✓ Builds optimized backend (production build)          │
│   ✓ Creates smaller, faster images                       │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Start Production Services                       │
│   docker compose -f docker-compose.prod.yml up -d        │
│                                                          │
│   What happens:                                          │
│   ✓ Starts PostgreSQL (production config)               │
│   ✓ Starts Redis (production config)                    │
│   ✓ Starts Backend (production mode)                     │
│   ✓ Starts Frontend (Nginx serving static files)         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Verify Deployment                               │
│   docker compose -f docker-compose.prod.yml ps          │
│   curl http://localhost:3000/api/health                  │
│   curl http://localhost:8080/health                      │
│                                                          │
│   All should return success/healthy                     │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Set Up Reverse Proxy (Nginx)                    │
│   - Configure Nginx to point to containers              │
│   - Set up SSL certificates (Let's Encrypt)             │
│   - Configure domain names                              │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 6: Test Production                                 │
│   - Access via domain name                              │
│   - Test all features                                   │
│   - Monitor logs                                         │
│   - Check performance                                    │
└─────────────────────────────────────────────────────────┘

Total Time: 15-30 minutes (first time)
Total Time: 5-10 minutes (updates)
```

---

## 🐛 Troubleshooting Workflow

```
┌─────────────────────────────────────────────────────────┐
│              SOMETHING IS BROKEN                        │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Check Service Status                            │
│   docker compose -f docker-compose.dev.yml ps          │
│                                                          │
│   Look for:                                              │
│   ✗ "Exited" = Container crashed                       │
│   ✗ "Restarting" = Infinite crash loop                 │
│   ✗ "unhealthy" = Health check failing                 │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Check Logs                                      │
│   docker compose -f docker-compose.dev.yml logs         │
│   docker compose -f docker-compose.dev.yml logs backend │
│                                                          │
│   Look for:                                              │
│   ✗ Error messages                                      │
│   ✗ Connection failures                                 │
│   ✗ Missing files                                        │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Identify the Problem                           │
│                                                          │
│   Common issues:                                         │
│   • Port conflict → Change port or stop conflict       │
│   • Database not ready → Wait or restart postgres       │
│   • Missing env vars → Check .env file                  │
│   • Code error → Check application logs                 │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Apply Fix                                        │
│                                                          │
│   Based on problem:                                      │
│   • Restart service: docker compose restart <service>   │
│   • Rebuild: docker compose build <service>             │
│   • Fix config: Edit docker-compose.yml or .env        │
│   • Fix code: Edit source files                         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Verify Fix                                      │
│   docker compose -f docker-compose.dev.yml ps           │
│   docker compose -f docker-compose.dev.yml logs --tail 20│
│                                                          │
│   Check: Everything should be "Up" and "healthy"       │
└─────────────────────────────────────────────────────────┘
```

---

## 🏢 Multi-Tenant Database Creation Workflow

```
┌─────────────────────────────────────────────────────────┐
│        CREATING NEW AGENCY (via API)                    │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: API Call                                         │
│   POST /api/agencies                                    │
│   {                                                     │
│     "agencyName": "New Company",                       │
│     "domain": "newcompany",                            │
│     "adminEmail": "admin@newcompany.com",              │
│     "adminPassword": "secure_password",                 │
│     "subscriptionPlan": "professional"                  │
│   }                                                     │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Backend Creates Agency Record                  │
│   - Inserts into main database (buildflow_db)          │
│   - Creates agency record in agencies table             │
│   - Generates database name: agency_newcompany_12345678 │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Backend Creates Isolated Database              │
│   CREATE DATABASE "agency_newcompany_12345678";         │
│                                                          │
│   What happens:                                          │
│   ✓ New empty database created                          │
│   ✓ Completely isolated from other agencies             │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Backend Creates Complete Schema                │
│   await createAgencySchema(client);                     │
│                                                          │
│   What gets created:                                     │
│   ✓ 53+ tables (users, profiles, projects, etc.)        │
│   ✓ Indexes for performance                             │
│   ✓ Functions and triggers                              │
│   ✓ Complete ERP schema                                 │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Backend Creates Admin User                      │
│   - Creates user in agency database                     │
│   - Creates profile with admin role                      │
│   - Sets up initial settings                            │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 6: Agency Ready!                                   │
│   - Admin can log in                                    │
│   - Complete ERP functionality available                │
│   - Data completely isolated                           │
└─────────────────────────────────────────────────────────┘

Total Time: 30 seconds - 2 minutes
```

---

## 🔄 Update Production Workflow

```
┌─────────────────────────────────────────────────────────┐
│         UPDATING PRODUCTION                             │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Backup Current Production                       │
│   docker compose -f docker-compose.prod.yml exec postgres │
│     pg_dump -U postgres buildflow_db >                  │
│     backup_before_update_$(date +%Y%m%d).sql            │
│                                                          │
│   Why: Safety net in case update fails                  │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Pull Latest Code                                │
│   git pull origin main                                  │
│                                                          │
│   Gets: Latest code changes                             │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Rebuild Images                                  │
│   docker compose -f docker-compose.prod.yml build        │
│                                                          │
│   What happens:                                          │
│   ✓ Builds with latest code                             │
│   ✓ Updates dependencies if changed                      │
│   ✓ Creates new optimized images                         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Deploy (Zero Downtime)                          │
│   docker compose -f docker-compose.prod.yml up -d        │
│                                                          │
│   What happens:                                          │
│   ✓ Stops old containers                                 │
│   ✓ Starts new containers                                │
│   ✓ Services reconnect automatically                     │
│   ✓ Minimal downtime (seconds)                           │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Verify Update                                   │
│   docker compose -f docker-compose.prod.yml ps           │
│   curl http://localhost:3000/api/health                  │
│   # Test application functionality                       │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 6: Monitor (First 10 minutes)                      │
│   docker compose -f docker-compose.prod.yml logs -f     │
│                                                          │
│   Watch for:                                             │
│   ✗ Errors in logs                                      │
│   ✗ High resource usage                                 │
│   ✗ Connection issues                                    │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 7: Rollback (If Issues)                            │
│   git checkout previous-version                          │
│   docker compose -f docker-compose.prod.yml build        │
│   docker compose -f docker-compose.prod.yml up -d        │
└─────────────────────────────────────────────────────────┘

Total Time: 10-15 minutes
Downtime: 5-30 seconds
```

---

## 📊 Monitoring Workflow

```
┌─────────────────────────────────────────────────────────┐
│            MONITORING YOUR SYSTEM                       │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 1: Check Service Status                            │
│   docker compose -f docker-compose.dev.yml ps           │
│                                                          │
│   All should show: "Up" and "healthy"                   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Check Resource Usage                            │
│   docker stats                                          │
│                                                          │
│   Watch for:                                             │
│   • High CPU (>80%) = Need optimization                 │
│   • High Memory (>80%) = Need more RAM or cleanup       │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Check Logs for Errors                           │
│   docker compose -f docker-compose.dev.yml logs --tail 100│
│                                                          │
│   Look for:                                              │
│   ✗ ERROR messages                                      │
│   ✗ WARN messages (investigate)                         │
│   ✓ Normal operation messages                           │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Check Database Health                           │
│   docker compose -f docker-compose.dev.yml exec postgres │
│     psql -U postgres -d buildflow_db -c                  │
│     "SELECT COUNT(*) FROM agencies;"                    │
│                                                          │
│   Verify: Database is accessible and responding          │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Check Application Health                        │
│   curl http://localhost:3000/api/health                 │
│   curl http://localhost:5173                            │
│                                                          │
│   Both should return success                            │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Quick Decision Tree

### "I want to..."

**Start working:**
→ `docker compose -f docker-compose.dev.yml up -d`

**Stop working:**
→ `docker compose -f docker-compose.dev.yml down`

**See what's happening:**
→ `docker compose -f docker-compose.dev.yml logs -f`

**Something broke:**
→ `docker compose -f docker-compose.dev.yml logs` (check logs)
→ `docker compose -f docker-compose.dev.yml ps` (check status)

**Changes not showing:**
→ `docker compose -f docker-compose.dev.yml restart backend`
→ `docker compose -f docker-compose.dev.yml restart frontend`

**Added new package:**
→ `docker compose -f docker-compose.dev.yml build backend`
→ `docker compose -f docker-compose.dev.yml up -d backend`

**Backup database:**
→ `docker compose -f docker-compose.dev.yml exec postgres pg_dump -U postgres buildflow_db > backup.sql`

**Deploy to production:**
→ `docker compose -f docker-compose.prod.yml build`
→ `docker compose -f docker-compose.prod.yml up -d`

---

**Remember:** These workflows are your roadmap. Follow them step-by-step, and you'll master Docker! 🚀
